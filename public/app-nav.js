/* ═══════════════════════════════════════════════════════════════════════════════
   EMPYREAN  —  app-nav.js  v3.0
   Navigation Engine + Upload Bridge + Close Button Wiring
   ═══════════════════════════════════════════════════════════════════════════════

   WHY THIS FILE EXISTS
   ─────────────────────
   app-fixes.js defines buildSidebar / navigateTo / buildHeader / renderDynamicUI
   as LOCAL functions inside a DOMContentLoaded closure. They are exposed to
   window at the very end of that closure, but app-auth.js calls them BEFORE
   app-fixes.js has finished executing. This file installs lightweight stubs
   that queue calls, then drains the queue once the real functions arrive.

   It also owns two cross-cutting concerns that broke because they were
   scattered across multiple files with competing event handlers:

   1. UPLOAD BUTTONS  — Every <label for="…"> that wraps a hidden file <input>
      must NOT have its default click behaviour cancelled. The master body
      click handler in app-fixes.js calls e.preventDefault() on many clicks
      before checking whether the target is a label. This file adds an early
      capture-phase listener that lets label→input clicks through first.

   2. CLOSE / EXIT BUTTONS — Same issue: close-modal × buttons sit inside
      .modal-overlay-container elements. The body listener fires e.preventDefault
      for many selectors without explicitly allowing close buttons that are
      children of non-matching ancestors. This file adds a capture-phase
      handler that runs before the bubbling handlers in app-fixes.js.

   LOAD ORDER: after app-helper.js, BEFORE app-auth.js
   ═══════════════════════════════════════════════════════════════════════════════ */

(function empyreanNav() {
    'use strict';

    /* ── 0. BRANDING (dashboard header logo) ─────────────────────────────────
       The round badge next to the notification bell on the Dashboard header.
       Admins can override it from Admin Panel → Branding, which writes to
       Firestore app_config/branding.headerLogoDataUrl (see app-admin.js,
       initAdminBrandingPanel). This block is the runtime half: it keeps
       window._empBranding in sync via a live onSnapshot listener and exposes
       window._empCurrentLogoSrc() so buildHeader() (and the admin panel's
       "CURRENT" preview) always show the live value — falling back to the
       shipped default logo when no override has been saved. ────────────── */
    var _EMP_DEFAULT_LOGO_SRC = 'data:image/png;base64,';

    var _empBranding = null;                    /* { headerLogoDataUrl: string|null } — synced live from Firestore */
    Object.defineProperty(window, '_empBranding', { get: function () { return _empBranding; }, configurable: true });
    var _brandingListenerStarted = false;

    function _empCurrentLogoSrc() {
        if (_empBranding && _empBranding.headerLogoDataUrl) return _empBranding.headerLogoDataUrl;
        return _EMP_DEFAULT_LOGO_SRC;
    }
    window._empCurrentLogoSrc = _empCurrentLogoSrc;

    function _startBrandingListener() {
        if (_brandingListenerStarted) return;
        if (!window.fbDb || typeof window.fbDb.collection !== 'function') return;
        _brandingListenerStarted = true;
        try {
            window.fbDb.collection('app_config').doc('branding').onSnapshot(function (snap) {
                _empBranding = (snap && snap.exists) ? (snap.data() || {}) : null;
                // Re-render the header live so every open tab/session picks
                // up a new logo the instant an admin saves one, no reload.
                if (typeof window.buildHeader === 'function') window.buildHeader();
                // Keep the admin panel's "CURRENT" preview in sync too, if open.
                var curImg = document.getElementById('admin-branding-current-logo');
                if (curImg) curImg.src = _empCurrentLogoSrc();
            }, function (err) {
                console.warn('[Nav] Branding listener error:', err && err.message);
            });
        } catch (e) {
            console.warn('[Nav] Could not start branding listener:', e);
        }
    }
    // Firebase may not be ready the instant this file executes; retry a few
    // times on a short interval, same pattern used elsewhere in this file
    // for the fbDb-dependent listeners.
    (function _brandingListenerBoot() {
        _startBrandingListener();
        if (_brandingListenerStarted) return;
        var tries = 0;
        var t = setInterval(function () {
            tries++;
            _startBrandingListener();
            if (_brandingListenerStarted || tries > 40) clearInterval(t);
        }, 500);
    })();

    /* ── 1. NAV ITEM REGISTRY ──────────────────────────────────────────────── */
    var NAV = [
        { id: 'dashboard',       label: 'Dashboard',       icon: 'fa-border-all'              },
        { id: 'my-wallet',       label: 'My Wallet',       icon: 'fa-wallet'           },
        { id: 'messages',        label: 'Messages',        icon: 'fa-comment-dots'                 },
        { id: 'marketplace',     label: 'Marketplace',     icon: 'fa-shop'                 },
        { id: 'go-live',         label: 'Go Live',         icon: 'fa-broadcast-tower'                },
        { id: 'reels',           label: 'Reels',           icon: 'fa-film'           },
        { id: 'news',            label: 'News',            icon: 'fa-newspaper'                   },
        // FIX (2026-08-30 — "we already have trend and tags under Activity,
        // remove the duplication"): the dedicated sidebar entries added
        // just before this fix (see the removed comment/lines) gave two
        // separate paths to the same Trending/Tags functionality — this one
        // and the existing Activity one. Removed here, not the underlying
        // #trending / #tags content-sections or app-tags.js's widgets
        // themselves (still fully intact, in case Activity's own links
        // still navigate to those same section ids) — only this
        // now-redundant nav entry point.
        { id: 'saved-posts',     label: 'Saved',           icon: 'fa-bookmark'                     },
        { id: 'download-log',    label: 'Downloads',       icon: 'fa-download'                     },
        { id: 'business-page',   label: 'Business Page',   icon: 'fa-building'             },
        { id: 'community-tasks', label: 'Community Tasks', icon: 'fa-list-check'          },
        { id: 'request-help',    label: 'Request Help (SOS)', icon: 'fa-hand-holding-heart'      },
        { id: 'report-crisis',   label: 'Report Crisis',   icon: 'fa-triangle-exclamation'  },
        { id: 'donor-hub',       label: 'Sponsor a Cause', icon: 'fa-hand-holding-heart'  },
        { id: 'grant-portal',    label: 'Grant Portal',    icon: 'fa-file-contract'   },
        { id: 'ngo-partners',    label: 'NGO Partners',    icon: 'fa-earth-africa'                 },
        { id: 'profile',         label: 'My Profile',      icon: 'fa-circle-user'                  },
        { id: 'settings',        label: 'Settings',        icon: 'fa-gear'             },
        // FIX (request — "remove the What You Get Access To page from the
        // loading screen and relocate it to the navigation bar"): this used
        // to be #app-concept-screen, a full-screen overlay auto-shown to
        // every signed-out visitor right after the splash, before they'd
        // even reached the sign up / log in screen. It's now reachable on
        // demand from here instead — same overlay markup/content, opened
        // via window.__empShowConceptScreenFromNav() (index.html) — same
        // "special case, not navigateTo()" pattern used for 'help-center'
        // just below, in the click handler further down.
        //
        // FIX (3rd follow-up — "bring that ENTIRE loading page section to
        // the nav bar ... explain [reel/live broadcast/go live] like the
        // others, and don't add them individually to the nav bar"): two
        // earlier attempts here (expandable accordion with all 3, then
        // with just 1) both split this into separate nav tabs, which is
        // the opposite of what was actually asked — the request is for the
        // ONE nav item to keep opening the ONE full overview page (every
        // pillar card, this one included), not for extra top-level/nested
        // nav links. Reverted back to a single, non-expandable item. The
        // overview page itself already covers Reels/Live Broadcast
        // Channel/Go Live Streaming as pillar cards with full descriptions
        // (index.html's DETAILS.reels / .livebroadcast / .golive — those
        // were genuinely missing before and are now filled in, which is
        // the "explain them like the others" part).
        //
        // BUG FIX (follow-up — "What We Offer icon is broken"): the special
        // case described above was documented in this comment but never
        // actually added to the click handler below — this NAV entry's id,
        // 'about-empyrean', has no matching #about-empyrean content-section
        // anywhere in index.html, so every click fell through to the plain
        // navigateTo('about-empyrean', true) call at the bottom of the
        // handler, which just logs "[Nav] Section not found" and does
        // nothing visible. That silent no-op is what read as "broken" —
        // nothing was wrong with the icon glyph itself. Fixed by adding the
        // actual special case (see the click handler, right alongside the
        // existing 'help-center' one it was always meant to mirror).
        // BUG FIX (follow-up — "What We Offer icon is broken", confirmed
        // via screenshot: renders as a tofu/missing-glyph box, not the
        // info-circle icon): 'fa-circle-info' is the FA6 name for this
        // icon, but the actual FontAwesome font file being served here
        // doesn't contain that specific glyph — every other icon in this
        // list ('fa-circle-user', 'fa-gear', 'fa-life-ring', etc.) renders
        // fine, so this is isolated to this one icon name, not a font-load
        // failure. Swapped to 'fa-info-circle', the older FA5 name for the
        // same glyph — kept as an alias in every FA6 build, so it resolves
        // regardless of exactly which font revision is actually loaded.
        { id: 'about-empyrean',  label: 'What We Offer',   icon: 'fa-info-circle' },
        { id: 'help-center',     label: 'Help & Assistance Center', icon: 'fa-life-ring' },
        { id: 'admin',           label: 'Admin Panel',     icon: 'fa-shield-halved', adminOnly: true }
    ];

    var MOBILE_NAV = [
        { id: 'dashboard',   label: 'Home',    icon: 'fa-border-all'    },
        { id: 'messages',    label: 'Messages',icon: 'fa-comment-dots'       },
        { id: 'marketplace', label: 'Market',  icon: 'fa-shop'       },
        { id: 'reels',       label: 'Reels',   icon: 'fa-film' },
        { id: 'profile',     label: 'Profile', icon: 'fa-circle-user'        }
    ];

    /* ── 2. HELPERS ────────────────────────────────────────────────────────── */
    function _isGuest() {
        /* BUG FIX: Check all authoritative sources in priority order.
           EmpState is the canonical source set by app-auth.js on login.
           fbAuth.currentUser is the ground truth from Firebase.
           window.isGuest is a fallback that may lag behind auth state. */
        if (window.EmpState && window.EmpState.isGuest === false) return false;
        if (window.fbAuth && window.fbAuth.currentUser) return false;
        if (window.EmpState && window.EmpState.isGuest === true) return true;
        if (window.isGuest === false) return false;
        if (window.isGuest === true)  return true;
        /* Default: guest until auth confirms otherwise */
        return true;
    }
    function _isAdmin()  { return !!( window.isAdmin  !== undefined ? window.isAdmin  : false ); }
    function _us()       { return window.userState || {}; }
    function _esc(s)     { return String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;'); }

    var _activeSection = 'dashboard';

    /* ── Per-section scroll-position memory ───────────────────────────────
       FIX (request — "in the marketplace, once a user clicks to view a
       card thumbnail, returning back should not take them to the top, but
       should return them to exactly where they were"): every section
       change shares ONE scroll container (.main-content — .content-section
       itself is just display:none/active, it doesn't scroll on its own),
       and navigateTo() below has always force-reset that container's
       scrollTop to 0 on every single call, including a Back-button pop
       (see the popstate listener just below) or returning from a page like
       Seller Profile (app-marketplace-sellers.js's _goTo -> navigateTo).
       That's the actual mechanism: leaving Marketplace scrolled halfway
       down, visiting another section, then coming back always landed back
       at the top, discarding exactly where the person was.
       FIX: remember each section's scrollTop the moment it's LEFT (keyed
       by section id, in this one map — no per-section wiring needed
       anywhere else), and restore it when that same section becomes
       active again. A section with no remembered position (the normal
       case — first-ever visit, or a genuinely new destination) still
       starts at 0, exactly as before; this only changes what happens when
       returning to a section already visited this session. */
    var _sectionScrollPositions = {};

    /* ── Hardware/browser Back button support ────────────────────────────
       FIX (request — "Enable users to navigate using the phone's back or
       exit button. When the back button is pressed, the current tab
       should close and the user should be taken to the previous
       section."): navigateTo() is the ONE authoritative section switcher
       (see file header), but until now it never touched browser history —
       pressing a hardware/back-gesture Back button just exited the app
       (or did nothing in a PWA/WebView shell) instead of stepping back
       through the sections the person had actually visited.
       This mirrors a pattern already established per-feature elsewhere in
       this codebase (see app-marketplace.js's mktDeepLink pushState/
       popstate pair for its own sub-tabs, and app-patch-openchat.js's
       _ocChatOpen pushState/popstate pair for the chat overlay) — applied
       here at the section-switcher level so it covers every section
       (Marketplace, Messages, Seller Profile, Business Page, Settings,
       etc.) generically, without each one needing its own history wiring.
       Every REAL section change (a different id than the one already
       active, and not itself a Back-button pop) pushes one history entry
       carrying { empSection: id }. Landing back on an earlier entry that
       carries that same marker re-runs navigateTo() for the section it
       names, without pushing a new entry (so it can't create a forward/
       back loop). History entries pushed by OTHER features (marketplace's
       own mktDeepLink, the openchat overlay's _ocChatOpen, the listing-
       detail page's mktListing, etc.) have no empSection key and are
       deliberately left alone here — each of those already owns its own
       popstate listener for its own sub-navigation; this one only acts
       when the browser lands on a state that names a top-level SECTION
       change, so the two layers can't fight over the same Back press. */
    var _navBackNavInProgress = false;
    window.addEventListener('popstate', function (e) {
        var st = e.state;
        if (st && st.empSection && st.empSection !== _activeSection) {
            _navBackNavInProgress = true;
            navigateTo(st.empSection, false);
            _navBackNavInProgress = false;
        }
    });

    /* FIX (2026-09-03 — "phone's Back button used to work, now completely
       stops responding"): navigateTo()'s own pushState call below only
       ever ADDS a new history entry on top of whatever's already there —
       it never touches the ORIGINAL entry that already existed the
       instant this page finished loading, before navigateTo() had been
       called even once. That original entry carries no empSection marker
       at all (it's whatever the browser set by default, effectively
       null). Walk the Back button through every section visited this
       session and it eventually pops all the way down to that one
       original, unmarked entry — at which point the popstate listener
       just above finds `st.empSection` missing, its `if` guard silently
       no-ops, and NOTHING updates. From the person's side this looks
       exactly like "the Back button stopped working": every press before
       that point correctly stepped back a section, and the one that
       finally reaches the untagged floor entry (and every press after
       it, since there's nothing further back in THIS page's history to
       land on) does nothing at all — not a crash, just silence, because
       there was never anything for the listener to react to. This is why
       it "used to work" (every tagged entry above the floor works fine)
       and then "completely stopped" (once the floor is reached, it stays
       broken for the rest of that session).
       FIX: the moment navigateTo() is first called at all (boot restore
       of the last-visited section — see index.html's own
       `window.navigateTo(last, true)` call), replaceState the CURRENT
       (original, pre-existing) entry so it carries the same
       {empSection:id} marker every other entry gets — replaceState edits
       the entry in place rather than adding a new one, so this doesn't
       change the size of the history stack or how many Back presses it
       takes to leave the app, it just makes the bottom of the stack
       recognizable to the listener above like every entry above it
       already is. */
    var _navRootHistoryTagged = false;
    function _tagRootHistoryEntryOnce(id) {
        if (_navRootHistoryTagged) return;
        _navRootHistoryTagged = true;
        try { history.replaceState({ empSection: id }, '', location.href); }
        catch (e) { /* replaceState can't fail in a way that should block navigation */ }
    }

    /** Stop and unload every playing video that is NOT inside the reel-viewer
     *  or go-live modals (those have their own teardown logic). */
    function _stopAllVideos() {
        document.querySelectorAll('video').forEach(function(v) {
            // Skip reel viewer and live stream — they manage their own video lifecycle
            if (v.closest('#reel-viewer-modal-overlay, #reel-viewer-container, ' +
                          '#go-live-modal-overlay, #live-stream-container')) return;
            try {
                if (!v.paused) v.pause();
            } catch(e) {}
        });
    }

    function navigateTo(id, fromClick) {
        if (!id) id = 'dashboard';
        // alias map
        var aliases = { home:'dashboard', feed:'dashboard', chat:'messages',
                        wallet:'my-wallet', live:'go-live', sos:'request-help',
                        saved:'saved-posts', bookmarks:'saved-posts',
                        downloads:'download-log' };
        id = aliases[id] || id;

        var target = document.getElementById(id);
        if (!target) { console.warn('[Nav] Section not found:', id); return; }

        // See the "FIX (2026-09-03 — phone's Back button..." comment above
        // the popstate listener for the full explanation: tags the
        // ORIGINAL (pre-existing) history entry with this section on the
        // very first call, so the Back button never bottoms out on an
        // untagged entry the popstate listener can't react to. A no-op on
        // every call after the first (guarded inside the function itself).
        _tagRootHistoryEntryOnce(id);

        // Section actually changing? (computed BEFORE _activeSection is
        // reassigned below) — used at the end to decide whether this call
        // needs its own history entry at all. Re-navigating to the
        // section that's already open (e.g. re-clicking the same sidebar
        // item) shouldn't stack a redundant Back-button step.
        var _sectionChanged = (id !== _activeSection);

        // Stop any playing videos in the section we are leaving
        _stopAllVideos();

        // Remember the OUTGOING section's scroll position before we switch
        // away from it (and before target.scrollTop reset below), but only
        // when the section is actually changing — re-navigating to the
        // section already open shouldn't overwrite its saved position with
        // whatever it happens to be mid-scroll at that exact instant.
        if (_sectionChanged) {
            var _mcOut = document.querySelector('.main-content');
            if (_mcOut) _sectionScrollPositions[_activeSection] = _mcOut.scrollTop;
        }

        document.querySelectorAll('.content-section').forEach(function(s){ s.classList.remove('active'); });
        target.classList.add('active');
        _activeSection = id;

        // sidebar active link  (data-target used by app-fixes.js, data-section used by this module)
        document.querySelectorAll('.sidebar-nav .nav-link').forEach(function(a){
            a.classList.toggle('active',
                a.dataset.target === id || a.dataset.section === id);
        });

        // mobile bottom nav
        var mbn = document.getElementById('mobile-bottom-nav');
        if (mbn) {
            mbn.querySelectorAll('.mobile-nav-item').forEach(function(b){
                b.classList.toggle('active', b.dataset.target === id || b.dataset.section === id);
            });
        }

        // breadcrumb
        var bc = document.getElementById('breadcrumb-text');
        if (bc) {
            var labels = {};
            NAV.forEach(function(n){ labels[n.id] = n.label; });
            bc.textContent = labels[id] || id;
        }

        // quick-post FAB
        var fab = document.getElementById('quick-post-fab');
        if (fab) fab.style.display = (id === 'dashboard' && !_isGuest()) ? 'flex' : 'none';

        // FIX (2026-08-10): hide all floating FAB buttons (Report/Complaint,
        // Help & Assistance) while on the Reels (Shorts) section. These are
        // position:fixed elements declared near the top of <body> in
        // index.html, well before any .content-section in the DOM, so they
        // can't be targeted with a CSS sibling/:has() selector scoped off
        // '#reels.content-section.active' the way #reels-fixed-topbar can
        // be positioned relative to the reel card. Same body-class pattern
        // already used for body.emp-live-active / body.emp-reel-active (see
        // style.css) — those cover the fullscreen live-stream and reel-
        // viewer overlays; this one covers the reel GRID section itself,
        // which those two don't reach. quick-post-fab is already handled
        // just above (dashboard-only), so it needs no extra rule here.
        document.body.classList.toggle('emp-section-reels', id === 'reels');

        // GAP FIX (permanent, this session): #reels-fixed-topbar has zero
        // rendered height while section#reels is display:none (its
        // .content-section parent isn't .active yet), so app-reel.js's own
        // _syncReelsTopbar() — which only runs on page load, window resize,
        // or a reel pin/unpin — silently no-ops on that first measurement
        // and --reels-topbar-h is left at its 96px CSS fallback. The very
        // first time a person opens the Reels tab in a session, that
        // fallback almost never matches the topbar's real rendered height,
        // producing a visible gap (or overlap) between the status bar and
        // the reel grid below it, until something else happens to trigger a
        // resync (resizing the window, pinning a reel). This is the root
        // cause of the gap "coming back" after every previous fix: those
        // fixes corrected the math, not the missing trigger. Section#reels
        // is already .active (rendered, real height) by this point in
        // navigateTo(), so re-running the sync exactly here — every time
        // the Reels tab is opened, not just on load/resize/pin — keeps
        // --reels-topbar-h correct from the very first frame, permanently.
        if (id === 'reels' && typeof window._empReelsApplyActiveOrdering === 'function') {
            window._empReelsApplyActiveOrdering();
        }

        // close sidebar on mobile
        var sb = document.querySelector('.sidebar');
        var ov = document.getElementById('content-overlay');
        if (sb && sb.classList.contains('open') && window.innerWidth <= 992) {
            sb.classList.remove('open');
            if (ov) ov.classList.remove('show');
            document.body.classList.remove('modal-open');
        }

        // Restore this section's remembered scroll position if it has one
        // (a section already visited this session — e.g. coming back to
        // Marketplace via the hardware Back button, or from Seller
        // Profile's own back action); otherwise this is a genuinely new
        // destination, so it starts at the top exactly as before. Only
        // touches scroll at all when the section actually changed — a
        // redundant re-navigate to the section already open (e.g.
        // re-clicking its already-active sidebar/tab link) now leaves
        // whatever the person is currently looking at alone, instead of
        // yanking them to an old saved (or top) position for no reason.
        if (_sectionChanged) {
            var mc = document.querySelector('.main-content');
            if (mc) {
                var _savedTop = _sectionScrollPositions[id];
                mc.scrollTop = (typeof _savedTop === 'number') ? _savedTop : 0;
            }
        }

        try { localStorage.setItem('empyrean_last_section', id); } catch(e){}
        try { document.dispatchEvent(new CustomEvent('empyrean-section-change',{detail:{section:id}})); } catch(e){}

        // Back-button history entry — see the "Hardware/browser Back button
        // support" comment above this function for the full explanation.
        // Only pushed when the section genuinely changed and this call
        // wasn't itself triggered by popping back to an earlier entry
        // (avoids stacking a duplicate/forward-looping history entry).
        if (_sectionChanged && !_navBackNavInProgress) {
            try { history.pushState({ empSection: id }, '', location.href); }
            catch(e) { /* pushState can't fail in a way that should block navigation */ }
        }

        // notify app-fixes.js listeners / admin panel
        if (typeof window.updateQuickPostFab === 'function') window.updateQuickPostFab(id);
        if (typeof window._startRealtimeListeners === 'function' && !_isGuest()) {
            var need = !window._postsListener || !window._mktListener || !window._newsListener;
            if (need) setTimeout(window._startRealtimeListeners, 100);
        }
    }
    window.navigateTo = navigateTo;

    /* ── 4. buildSidebar ───────────────────────────────────────────────────── */
    // FIX (request — "add reel / live broadcast channel / go live streaming
    // section under What We Offer, clicking the tab should open up the
    // section"): routes each of the 3 expandable children to its real,
    // live destination — not the info overlay. "Reels" and "Go Live
    // Streaming" are ordinary content-sections, so those two are just
    // navigateTo(). "Live Broadcast Channel" isn't its own section — it's
    // the composer panel INSIDE Reels, opened by clicking
    // #reels-connect-live-btn (see app-reel.js's own document click
    // delegation for that id). That button only exists once #reels has
    // actually rendered, so this navigates there first and then polls
    // briefly for the button rather than assuming it's already in the DOM
    // (same "poll briefly after a section swap" pattern already used
    // elsewhere in this codebase, e.g. app-patch-v37.js's own footer-button
    // wiring) — it also naturally waits out the guest-gate check that
    // button's own click handler already does (shows its own "log in"
    // notice for guests), so nothing here needs to duplicate that check.
    function _openWhatWeOfferChild(child) {
        navigateTo(child.target, true);
        if (!child.openLiveBroadcast) return;

        var tries = 0;
        (function tryOpenLiveBroadcastBtn() {
            tries++;
            var liveBtn = document.getElementById('reels-connect-live-btn');
            if (liveBtn) { liveBtn.click(); return; }
            if (tries < 20) setTimeout(tryOpenLiveBroadcastBtn, 150);
        })();
    }

    function buildSidebar() {
        var ul = document.querySelector('.sidebar-nav');
        var footerEl = document.querySelector('.sidebar-footer');
        if (!ul) return;

        var guest = _isGuest();
        var admin = _isAdmin();
        var us    = _us();

        // Build nav items
        ul.innerHTML = '';
        NAV.forEach(function(item) {
            if (item.adminOnly && !admin) return;
            if (['my-wallet','messages','go-live','business-page','community-tasks',
                 'request-help','report-crisis','donor-hub','profile','settings','saved-posts','download-log'].indexOf(item.id) !== -1
                && guest) return;

            var hasChildren = !!(item.children && item.children.length);
            // A child is active whenever its real target section is the
            // active one (matches navigateTo()'s own active-link logic
            // below), so the group auto-expands to reveal which child is
            // currently open instead of hiding the active link inside a
            // collapsed group.
            var activeChild = hasChildren
                ? item.children.filter(function(c){ return c.target === _activeSection; })[0]
                : null;

            var li = document.createElement('li');
            if (hasChildren) li.className = 'nav-item--expandable' + (activeChild ? ' nav-item--open' : '');
            var a  = document.createElement('a');
            a.href = '#';
            a.className = 'nav-link' + (item.id === _activeSection ? ' active' : '');
            a.dataset.target  = item.id;   // app-fixes.js uses data-target
            a.dataset.section = item.id;   // this module uses data-section
            a.setAttribute('aria-label', item.label);
            if (hasChildren) {
                a.className += ' nav-link--expandable';
                a.setAttribute('aria-expanded', activeChild ? 'true' : 'false');
            }
            a.innerHTML =
                '<span class="nav-icon-box"><i class="fas ' + item.icon + '"></i></span>'
                + '<span style="flex:1;letter-spacing:0.01em;">' + item.label + '</span>'
                + (hasChildren ? '<i class="fas fa-chevron-right nav-expand-chevron"></i>' : '');

            a.addEventListener('click', function(e) {
                e.preventDefault();
                e.stopPropagation();

                // Expandable groups (currently just "What We Offer") toggle
                // open/closed in place instead of navigating anywhere or
                // opening the info overlay — see the children[] comment on
                // the NAV entry above for why.
                if (hasChildren) {
                    var nowOpen = li.classList.toggle('nav-item--open');
                    a.setAttribute('aria-expanded', nowOpen ? 'true' : 'false');
                    return;
                }

                // FIX (bug: "owner's own business page renders as a visitor
                // page"): window._activeBizPageId / window._activeBizData
                // are SHARED state also used to view OTHER users' pages
                // (dashboard cards, product-card slider, etc. all set them
                // before navigating). Those flows never reset them back —
                // so if the user last viewed someone else's page and THEN
                // clicks their own "Business Page" nav link, the stale id
                // was still pointing at the other page, whose ownerId never
                // matches, and the owner's controls never showed. Force it
                // back to the current user's own page — or clear it if they
                // don't have one — every time this link (not a card, not a
                // deep link) is what triggered the navigation.
                if (item.id === 'business-page') {
                    var ownerState = _us();
                    var ownPage = ownerState.businessPage
                        || (Array.isArray(ownerState.businessPages) && ownerState.businessPages[0])
                        || null;
                    window._activeBizPageId = ownPage ? (typeof ownPage === 'object' ? ownPage.id : ownPage) : '';
                    window._activeBizData   = (ownPage && typeof ownPage === 'object') ? ownPage : null;
                }
                // "Help & Assistance Center" isn't a real content-section —
                // it opens the self-help overlay (self-help-assistance-
                // center.js) directly instead of calling navigateTo, which
                // has no matching section to show for it.
                if (item.id === 'help-center') {
                    if (typeof window._empOpenHelpCenter === 'function') {
                        window._empOpenHelpCenter();
                    }
                    return;
                }
                // "What We Offer" isn't a real content-section either — it
                // opens the same full-screen overview overlay a signed-out
                // visitor sees after the splash (index.html's
                // #app-concept-screen), via the nav-specific entry point
                // that overlay already exposes for this. See the NAV entry
                // above for the full history/why.
                if (item.id === 'about-empyrean') {
                    if (typeof window.__empShowConceptScreenFromNav === 'function') {
                        window.__empShowConceptScreenFromNav();
                    }
                    return;
                }
                navigateTo(item.id, true);
            });
            li.appendChild(a);

            if (hasChildren) {
                var subUl = document.createElement('ul');
                subUl.className = 'nav-sublist';
                item.children.forEach(function(child) {
                    var subLi = document.createElement('li');
                    var subA  = document.createElement('a');
                    subA.href = '#';
                    subA.className = 'nav-link nav-sublink' + (child.target === _activeSection ? ' active' : '');
                    subA.dataset.target  = child.target;
                    subA.dataset.section = child.target;
                    subA.setAttribute('aria-label', child.label);
                    subA.innerHTML =
                        '<span class="nav-icon-box"><i class="fas ' + child.icon + '"></i></span>'
                        + '<span style="flex:1;letter-spacing:0.01em;">' + child.label + '</span>';
                    subA.addEventListener('click', function(e) {
                        e.preventDefault();
                        e.stopPropagation();
                        _openWhatWeOfferChild(child);
                    });
                    subLi.appendChild(subA);
                    subUl.appendChild(subLi);
                });
                li.appendChild(subUl);
            }

            ul.appendChild(li);
        });

        // Build footer
        if (footerEl) _buildSidebarFooter(footerEl, guest, admin, us);

        console.log('[Nav] Sidebar built — ' + ul.children.length + ' items');
    }
    window.buildSidebar = buildSidebar;

    function _buildSidebarFooter(el, guest, admin, us) {
        var avatar = us.avatar || us.profilePic || '';

        /* ── Name repair: detect email-prefix artifacts at render time ──
           Only flag a fullName as "bad" (auto-generated) when it is clearly
           not a real name.  Specifically:
             1. Empty / missing
             2. The full email address ("john@gmail.com")
             3. Contains "@" (never a real name)
             4. Exactly the raw lowercase email prefix ("johndoe" or "john.doe")
                — all lowercase with no capitalisation means it was auto-generated
           BUG FIX: the old condition `(!_rawName.includes(' ') && _rawClean === _emailPfx)`
           was too broad — it flagged ANY single-word real name (e.g. "Emmanuel",
           "John") as wrong because after stripping/lowercasing it matched the
           email prefix.  A capitalised name is a real name; only reject it if it
           is literally the raw prefix string in its original form.             */
        var _rawName  = us.fullName || '';
        var _email    = (us.email || '').toLowerCase();
        var _rawPfx   = _email.split('@')[0];          // e.g. "john.doe" or "johndoe"
        var _emailPfx = _rawPfx.replace(/[^a-z0-9]/g,''); // stripped for fallback
        /* isExactRawPrefix: true only when fullName IS the lowercase prefix itself */
        var _isExactRawPrefix = (_rawName === _rawPfx) || (_rawName.toLowerCase() === _rawPfx);
        var _nameIsWrong = !_rawName
            || _rawName === _email
            || _rawName.indexOf('@') !== -1
            || _isExactRawPrefix;
        if (_nameIsWrong && _email) {
            /* Try Firebase Auth displayName first (most authoritative) */
            var _fbDN = (window.fbAuth && window.fbAuth.currentUser &&
                         window.fbAuth.currentUser.displayName) || '';
            if (_fbDN && _fbDN !== _email && _fbDN !== _rawPfx
                    && _fbDN.indexOf('@') === -1) {
                us.fullName = _fbDN;
                if (window.userState) window.userState.fullName = _fbDN;
                if (window.EmpState && window.EmpState.userState)
                    window.EmpState.userState.fullName = _fbDN;
            } else {
                /* Fall back to localStorage signup data */
                try {
                    var _ls = JSON.parse(localStorage.getItem('empyrean_users') || '{}');
                    var _lsEntry = _ls[_email] || {};
                    var _lsName  = (_lsEntry.fullName || '').trim();
                    if (_lsName && _lsName !== _email && _lsName !== _rawPfx
                            && _lsName.indexOf('@') === -1) {
                        us.fullName = _lsName;
                        if (window.userState) window.userState.fullName = _lsName;
                        if (window.EmpState && window.EmpState.userState)
                            window.EmpState.userState.fullName = _lsName;
                    }
                } catch(e) {}
            }
        }

        var name   = us.fullName || us.username || (guest ? 'Guest' : 'User');
        var uname  = us.username ? '@' + us.username : (admin ? 'Administrator' : '');
        // FIX (bug: "bottom of the nav shows an email address instead of
        // the account owner's full name"): the repair block above only
        // ever sanitized us.fullName. If that repair couldn't find a real
        // name anywhere (no Firebase displayName, no localStorage signup
        // record) it silently fell through, and `name` here could still
        // end up as a raw email — either directly (us.fullName was never
        // set) or via the us.username fallback (some accounts have their
        // stored "username" field literally be their email). Never render
        // a raw email address in either line; fall back to a title-cased
        // version of the email's local-part rather than exposing it.
        var _emailLike = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        if (_emailLike.test(name)) {
            var _localPart = name.split('@')[0].replace(/[._-]+/g, ' ').trim();
            name = _localPart.replace(/\b\w/g, function (c) { return c.toUpperCase(); }) || (guest ? 'Guest' : 'User');
        }
        if (_emailLike.test(uname)) uname = '';
        var avatarHTML = avatar
            ? '<img src="'+_esc(avatar)+'" alt="avatar" style="width:44px;height:44px;border-radius:50%;object-fit:cover;border:2px solid rgba(245,197,24,0.6);" onerror="this.src=\'https://ui-avatars.com/api/?name='+encodeURIComponent(name)+'&background=1B2B8B&color=fff&size=80\'">'
            : '<div style="width:44px;height:44px;border-radius:50%;background:rgba(245,197,24,0.15);border:2px solid rgba(245,197,24,0.4);display:flex;align-items:center;justify-content:center;"><i class="fas fa-circle-user" style="color:rgba(245,197,24,0.8);font-size:1.1rem;"></i></div>';

        el.innerHTML = (!guest ? (
            '<div class="nav-link" data-target="'+(admin?'admin':'profile')+'" style="display:flex;align-items:center;gap:12px;padding:12px 14px;background:rgba(255,255,255,0.06);border-radius:12px;margin-bottom:10px;cursor:pointer;border:1px solid rgba(255,255,255,0.07);">'
            + '<div style="position:relative;flex-shrink:0;">' + avatarHTML
            + '<div style="position:absolute;bottom:0;right:0;width:11px;height:11px;background:#10B981;border-radius:50%;border:2px solid #1a1a2e;"></div></div>'
            + '<div style="flex:1;min-width:0;">'
            + '<div style="font-weight:700;font-size:0.87rem;color:white;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">' + _esc(name) + '</div>'
            + '<div style="font-size:0.72rem;color:rgba(255,255,255,0.4);">' + _esc(uname) + '</div></div>'
            + '<i class="fas fa-chevron-right" style="color:rgba(255,255,255,0.2);font-size:0.7rem;flex-shrink:0;"></i></div>'
            + '<a href="#" id="logout-btn" style="display:flex;align-items:center;gap:10px;padding:10px 14px;background:rgba(239,68,68,0.08);border:1px solid rgba(239,68,68,0.18);border-radius:10px;color:#FCA5A5;font-weight:600;font-size:0.83rem;text-decoration:none;margin-bottom:10px;"><i class="fas fa-sign-out-alt"></i> Sign Out</a>'
        ) : (
            '<button id="login-signup-btn" class="btn btn-accent" style="width:100%;margin-bottom:10px;padding:12px;font-size:0.9rem;font-weight:700;"><i class="fas fa-sign-in-alt"></i> Login / Sign Up</button>'
        ))
        + '<div class="social-links" style="display:flex;justify-content:center;gap:12px;flex-wrap:wrap;">'
        + '<a href="https://www.youtube.com/@EmpyreanHFNewsTV" target="_blank" title="YouTube"><svg viewBox="0 0 24 24" width="1em" height="1em" fill="none" stroke="currentColor" stroke-width="1.8"><rect x="2" y="5" width="20" height="14" rx="5"/><path d="M10 8.7v6.6l6-3.3z" fill="currentColor" stroke="none"/></svg></a>'
        + '<a href="https://x.com/EmpyToken" target="_blank" title="X / Twitter"><svg viewBox="0 0 24 24" width="1em" height="1em" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round"><path d="M4 4l16 16M20 4L4 20"/></svg></a>'
        + '<a href="https://www.instagram.com/empyreantoken_empy" target="_blank" title="Instagram"><svg viewBox="0 0 24 24" width="1em" height="1em" fill="none" stroke="currentColor" stroke-width="1.8"><rect x="3" y="3" width="18" height="18" rx="5"/><circle cx="12" cy="12" r="4.2"/><circle cx="17.2" cy="6.8" r="1.1" fill="currentColor" stroke="none"/></svg></a>'
        + '<a href="https://www.linkedin.com/company/108660039" target="_blank" title="LinkedIn"><svg viewBox="0 0 24 24" width="1em" height="1em" fill="none" stroke="currentColor" stroke-width="1.8"><rect x="3" y="3" width="18" height="18" rx="4"/><circle cx="8.2" cy="8.5" r="1.15" fill="currentColor" stroke="none"/><path d="M8.2 11.2v6.3" stroke-width="2" stroke-linecap="round"/><path d="M12 17.5v-4c0-1.5.9-2.6 2.3-2.6 1.3 0 2 .9 2 2.5v4.1" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg></a>'
        + '<a href="https://t.me/EmpyreanToken" target="_blank" title="Telegram"><svg viewBox="0 0 24 24" width="1em" height="1em" fill="none" stroke="currentColor" stroke-width="1.8"><circle cx="12" cy="12" r="9.5"/><path d="M5 12.5l13.5-5.7-2 12-3.6-3-2.4 2.2-.3-3.4z" fill="currentColor" stroke="none"/></svg></a>'
        + '<a href="https://whatsapp.com/channel/0029VbAyfxaAzNc45vhje92j" target="_blank" title="WhatsApp"><svg viewBox="0 0 24 24" width="1em" height="1em" fill="none" stroke="currentColor" stroke-width="1.7"><path d="M12 3a9 9 0 0 0-7.8 13.5L3 21l4.7-1.2A9 9 0 1 0 12 3z"/><path d="M9.2 8.6a1 1 0 0 1 1.1-.3l1 .3a1 1 0 0 1 .7.9l.1 1a1 1 0 0 1-.3.8l-.4.4a6.8 6.8 0 0 0 3 3l.4-.4a1 1 0 0 1 .8-.3l1 .1a1 1 0 0 1 .9.7l.3 1a1 1 0 0 1-.3 1.1c-.6.5-1.5.8-2.3.6-2.6-.6-5.6-3.6-6.2-6.2-.2-.8.1-1.7.6-2.3z" fill="currentColor" stroke="none"/></svg></a>'
        + '</div>';

        // bind footer card click → profile / admin
        var card = el.querySelector('.nav-link[data-target]');
        if (card) {
            card.addEventListener('click', function(e) {
                e.preventDefault();
                navigateTo(card.dataset.target, true);
            });
        }
    }

    /* ── 5. buildHeader ────────────────────────────────────────────────────── */
    function buildHeader() {
        if (typeof window._populateHomeBioCard === 'function') window._populateHomeBioCard();
        var ha = document.getElementById('main-header-actions');
        if (!ha) return;
        // FIX (bug: "notification bell has stopped working"): this used to
        // blindly overwrite ha.innerHTML on every call. app-notifications.js
        // prepends the bell (with its bound click listeners) into this exact
        // same container — buildHeader() runs on every navigation/section
        // switch via renderDynamicUI(), so the bell (and its listeners) was
        // being destroyed the very first time the header re-rendered after
        // it appeared, with nothing bringing it back. The two pieces of code
        // were never actually in conflict feature-wise, just fighting over
        // the same innerHTML — preserve the bell's wrapper across rebuilds
        // instead of nuking it, and if it hasn't been created yet, ask
        // app-notifications.js to (re)build it right after.
        var bellWrap = ha.querySelector('#notif-bell-btn');
        bellWrap = bellWrap ? bellWrap.parentElement : null;
        // Official Empyrean Humanitarian Foundation emblem — embedded inline
        // (base64) rather than linked externally so this header icon never
        // depends on an outside asset host being reachable.
        // Admin-configurable header badge (see the BRANDING block near the
        // top of this file). Falls back to the shipped default logo when
        // no admin override has been saved to Firestore.
        var _empLogoSrc = _empCurrentLogoSrc();
        var empLink = '<a href="https://empyreanhumanitarianfoundation.com" target="_blank" rel="noopener" title="Empyrean Official Website" style="display:inline-flex;align-items:center;justify-content:center;width:36px;height:36px;border-radius:50%;overflow:hidden;background:linear-gradient(135deg,#0A0E27,#1B2B8B);border:2px solid rgba(245,197,24,0.6);cursor:pointer;text-decoration:none;flex-shrink:0;margin-right:6px;"><img src="' + _empLogoSrc + '" alt="Empyrean Humanitarian Foundation" style="width:100%;height:100%;object-fit:cover;display:block;"></a>';
        ha.innerHTML = _isGuest()
            ? empLink
            : empLink + '<button class="btn cart-icon-button" id="cart-icon-btn"><i class="fas fa-shopping-cart"></i> Cart <span class="cart-item-count">0</span></button>';
        if (bellWrap) {
            ha.prepend(bellWrap);
        } else if (typeof window.buildNotificationBell === 'function') {
            window.buildNotificationBell();
        }
    }
    window.buildHeader = buildHeader;

    /* ── 6. renderDynamicUI ────────────────────────────────────────────────── */
    function renderDynamicUI() {
        buildSidebar();
        buildHeader();
        _buildMobileBottomNav();
        var fab = document.getElementById('quick-post-fab');
        if (fab) fab.style.display = (_activeSection === 'dashboard' && !_isGuest()) ? 'flex' : 'none';
        if (typeof window.updateWalletUI === 'function') window.updateWalletUI();
        if (typeof window.updateCartUI   === 'function') window.updateCartUI();
    }
    window.renderDynamicUI = renderDynamicUI;

    /* ── 7. _buildMobileBottomNav ──────────────────────────────────────────── */
    function _buildMobileBottomNav() {
        var old = document.getElementById('mobile-bottom-nav');
        if (old) old.remove();

        var guest = _isGuest();
        var items = guest
            ? [
                { id:'dashboard',   label:'Home',    icon: 'fa-border-all'    },
                { id:'marketplace', label:'Market',  icon: 'fa-shop'       },
                { id:'reels',       label:'Reels',   icon: 'fa-film' },
                { id:'news',        label:'News',    icon: 'fa-newspaper'         },
                /* FIX (2026-08-30 — duplicate of the existing Activity path,
                   see app-nav.js's sidebar array for the full note): removed. */
                { id:'ngo-partners',label:'NGOs',    icon: 'fa-earth-africa'       }
              ]
            : [
                { id:'dashboard',      label:'Home',     icon: 'fa-border-all'           },
                { id:'reels',          label:'Reels',    icon: 'fa-film'        },
                { id:'news',           label:'News',     icon: 'fa-newspaper'                },
                { id:'marketplace',    label:'Market',   icon: 'fa-shop'              },
                /* FIX (2026-08-30 — duplicate of the existing Activity path,
                   see app-nav.js's sidebar array for the full note): removed. */
                { id:'report-crisis',  label:'Crisis',   icon: 'fa-triangle-exclamation'},
                { id:'messages',       label:'Messages', icon: 'fa-comment-dots'              },
                { id:'go-live',        label:'Go Live',  icon: 'fa-broadcast-tower'             },
                { id:'request-help',   label:'SOS',      icon: 'fa-hand-holding-heart'      },
                { id:'donor-hub',      label:'Sponsor',  icon: 'fa-hand-holding-heart'      },
                { id:'saved-posts',    label:'Saved',    icon: 'fa-bookmark'        },
                { id:'download-log',   label:'Downloads',icon: 'fa-download'        },
                { id:'my-wallet',      label:'Wallet',   icon: 'fa-wallet'        },
                { id:'profile',        label:'Profile',  icon: 'fa-circle-user'               }
              ];

        var nav = document.createElement('nav');
        nav.id = 'mobile-bottom-nav';
        nav.setAttribute('role', 'navigation');
        nav.setAttribute('aria-label', 'Bottom navigation');

        items.forEach(function(item) {
            var btn = document.createElement('button');
            btn.className = 'mobile-nav-item' + (item.id === _activeSection ? ' active' : '');
            btn.dataset.target  = item.id;
            btn.dataset.section = item.id;
            btn.setAttribute('aria-label', item.label);
            /* FEATURE (this session — "enable message viewers count
               indicator in the message chat icon"): only the 'messages'
               item gets the badge span — #nav-messages-badge, same
               unread-red-circle pattern app-notifications.js's own
               #notif-badge already uses on the bell icon. The icon is
               wrapped in a position:relative span so the badge (position:
               absolute) anchors to the icon itself, not the whole button.
               Content/visibility is owned by app-chat.js's
               renderContactList(), which already computes an unread count
               per contact on every render (including on every incoming/
               outgoing message) — this only adds the element for that
               function to update; it starts empty/hidden here. */
            btn.innerHTML = (item.id === 'messages'
                ? '<span style="position:relative;display:inline-flex;">'
                    + '<i class="fas ' + item.icon + '"></i>'
                    + '<span id="nav-messages-badge" style="display:none;position:absolute;top:-4px;right:-8px;'
                        + 'background:#EF4444;color:#fff;font-size:0.58rem;font-weight:800;min-width:15px;height:15px;'
                        + 'border-radius:50%;align-items:center;justify-content:center;border:1.5px solid #fff;padding:0 2px;">0</span>'
                  + '</span>'
                : '<i class="fas ' + item.icon + '"></i>')
                + '<span>' + item.label + '</span>';
            nav.appendChild(btn);
        });

        nav.addEventListener('click', function(e) {
            var btn = e.target.closest('.mobile-nav-item');
            if (!btn) return;
            e.preventDefault();
            e.stopPropagation();
            nav.querySelectorAll('.mobile-nav-item').forEach(function(b){ b.classList.remove('active'); });
            btn.classList.add('active');
            navigateTo(btn.dataset.target, true);
        });

        document.body.appendChild(nav);
        window._buildMobileBottomNav = _buildMobileBottomNav;
    }
    window._buildMobileBottomNav = _buildMobileBottomNav;

    /* ── 8. AUTH MODAL OPENER ──────────────────────────────────────────────── */
    window._openAuthModal = function(view) {
        var am = document.getElementById('auth-modal-overlay');
        var lv = document.getElementById('login-view');
        var sv = document.getElementById('signup-view');
        if (!am) return;
        am.style.display = 'flex'; am.classList.add('show');
        document.body.classList.add('modal-open');
        if (view === 'signup' && sv && lv) { sv.style.display='block'; lv.style.display='none'; }
        else if (lv) lv.style.display = 'block';
        setTimeout(function(){ if(typeof window.generateCaptcha==='function') window.generateCaptcha(); }, 80);
    };

    /* ── 8b. GUEST CONTENT-CREATION GATE ─────────────────────────────────────
       FIX (2026-08-04 — "make the site browsable without login, but block
       posting/uploading for guests, redirect to sign up"): companion to the
       app-auth.js change (same session) that stopped initializeApp(true)
       from force-opening the #auth-modal-overlay on every guest boot. Now
       that a signed-out visitor can freely browse (dashboard, marketplace,
       reels, news, NGO partners — exactly what app-nav.js's own guest-aware
       sidebar/mobile-nav above already scope them to), this is the one
       place that still needs to say "no": any attempt to actually CREATE
       or UPLOAD content.

       Registered on `window` (not `document`) in the CAPTURE phase — that
       is what makes this run before every other capture listener, in this
       file (§9/§10 below) or any other script, regardless of file/script
       load order. Capture propagates top-down: window → document → ... →
       target. §10 below already calls e.stopImmediatePropagation() on
       file-input label clicks, which would silently swallow a listener
       added anywhere else on `document` — attaching to `window` is what
       lets this gate see the click first, every time.

       WHAT COUNTS AS "GATED": virtually every upload anywhere in this app
       funnels through a <label for="fileInputId"> wrapping a hidden
       <input type="file"> (status, SOS, crisis reports, marketplace
       listings, complaint evidence, admin news/announcements, profile/
       cover photos — see §9's own comment two blocks down for the full
       list this pattern already covers) — catching that ONE pattern
       blocks every upload button in the app without a selector per
       feature. On top of that, a short curated list covers the handful of
       "create/post" actions that don't go through a file input at all (a
       text-only status update, starting a marketplace listing, going
       live, filing a crisis report, the quick-post FAB). Anything marked
       `data-guest-blocked` is also covered, as a forward-compatible hook
       for future buttons without needing to edit this list again. */
    var GUEST_BLOCKED_SELECTORS = [
        '#quick-post-fab',
        '#post-status-btn',
        '#add-my-status-btn',
        '#marketplace-sell-card',
        '#submit-complaint-fab',
        '#create-post-form button[type="submit"]',
        '#create-post-form input[type="submit"]',
        '#reel-upload-form button[type="submit"]',
        '#go-live-form button[type="submit"]',
        '#crisis-form button[type="submit"]',
        /* FIX (2026-08-04 — follow-up to the guest-gate above): these three
           don't go through a file upload at all, so the file-input-label
           gate never sees them, and none of them live in a static form
           with a fixed id in index.html the way create-post-form/
           reel-upload-form/go-live-form/crisis-form do — each is built at
           runtime by its own module, so a per-page-load id like
           'create-post-form' can't cover them. Their ELEMENT ids/classes
           are still stable across every re-render, though (verified by
           reading each render function directly), which is enough to gate
           on here without needing a change anywhere else:
             - .comment-form  — the per-post "Add a comment…" form
               app-thread.js stamps onto EVERY post/business-post card
               (same class reused on every card, by design — see that
               file's own _buildCard()-style templates); the submit button
               inside it is what actually writes the comment.
             - #biz-post-submit-btn — the "Post" button in the business
               page's own composer (app-business.js's #biz-post-composer,
               owner-only in principle, but reachable by any guest viewing
               someone else's business page before this gate). Note the
               composer's photo/video button is a normal
               label[for="biz-media-input"] and is already covered by the
               file-upload gate above without needing to be listed here.
             - #v13-gn-create — "Create group" in the new-group panel
               (app-patch-v13.js). Group chat itself is hidden from guests
               in the sidebar/mobile nav already; this is defense-in-depth
               in case the panel is ever reachable another way. */
        '.comment-form button[type="submit"]',
        '#biz-post-submit-btn',
        '#v13-gn-create',
        '[data-guest-blocked]'
    ].join(', ');
    var GUEST_BLOCKED_FORM_IDS = ['create-post-form', 'reel-upload-form', 'go-live-form', 'crisis-form'];
    var GUEST_BLOCKED_FORM_CLASSES = ['comment-form'];

    function _redirectGuestToSignup(reason) {
        if (typeof window.showNotification === 'function') {
            window.showNotification('Create a free account to ' + reason + ' — it only takes a moment.', 'info');
        }
        /* openAuthModal (app-auth.js) is the canonical opener; _openAuthModal
           (this file, §8) is a fallback in case app-auth.js hasn't defined
           its version yet for some reason — both land on the signup view. */
        if (typeof window.openAuthModal === 'function') window.openAuthModal('signup');
        else if (typeof window._openAuthModal === 'function') window._openAuthModal('signup');
    }

    window.addEventListener('click', function(e) {
        if (!_isGuest()) return;
        var t = e.target;
        if (!t || !t.closest) return;

        /* File upload: label -> input[type=file] (covers ~every upload
           button in the app) plus a direct click on the input itself
           (defensive, in case some control isn't wrapped in a label). */
        var label = t.tagName === 'LABEL' ? t : t.closest('label');
        var isFileLabel = false;
        if (label) {
            var forId = label.getAttribute('for') || label.dataset.uploadFor;
            var inp = forId ? document.getElementById(forId) : null;
            isFileLabel = !!(inp && inp.type === 'file');
        }
        var isFileInput = !!(t.matches && t.matches('input[type="file"]'));

        if (isFileLabel || isFileInput) {
            e.preventDefault();
            e.stopImmediatePropagation();
            _redirectGuestToSignup('upload photos or videos');
            return;
        }

        var blocked = t.closest(GUEST_BLOCKED_SELECTORS);
        if (blocked) {
            e.preventDefault();
            e.stopImmediatePropagation();
            _redirectGuestToSignup('post');
            return;
        }
    }, true /* capture, on window — see header comment for why */);

    /* Defense in depth: a keyboard Enter-submit on a gated form bypasses
       any click handler entirely. Same guest check, same window-capture
       trick, on the 'submit' event this time. Checks both a fixed id
       (the static forms) and a class (.comment-form, stamped onto many
       per-post forms that share one class but never a fixed id — see the
       GUEST_BLOCKED_SELECTORS comment above). */
    window.addEventListener('submit', function(e) {
        if (!_isGuest()) return;
        var form = e.target;
        if (!form) return;
        var idMatch    = GUEST_BLOCKED_FORM_IDS.indexOf(form.id) !== -1;
        var classMatch = form.classList && GUEST_BLOCKED_FORM_CLASSES.some(function(c) { return form.classList.contains(c); });
        if (!idMatch && !classMatch) return;
        e.preventDefault();
        e.stopImmediatePropagation();
        _redirectGuestToSignup('post');
    }, true);

    /* ── 9. UPLOAD BUTTONS — capture-phase, runs before body handler ───────── */
    /* Root cause of broken uploads:
       app-fixes.js attaches document.body.addEventListener('click', ...) with bubbling.
       Some paths inside that handler call e.preventDefault() before checking if the
       target is a <label>. Because labels rely on their default click-to-input behaviour,
       preventDefault() breaks them.

       Fix: attach a CAPTURE-PHASE listener on document. Capture fires before bubbling.
       If the click target is a <label[for]> that points to a file input, we let it
       through and mark the event so the body handler skips it. */
    document.addEventListener('click', function(e) {
        var t = e.target;
        var label = t.tagName === 'LABEL' ? t : t.closest('label');
        if (!label) return;

        var forId = label.getAttribute('for') || label.dataset.uploadFor;
        if (!forId) return;

        var inp = document.getElementById(forId);
        if (!inp || inp.type !== 'file') return;

        // Mark event so downstream handlers know it's a file-label click
        e._isFileLabelClick = true;
        // Don't call stopPropagation — we want the default label→input click to proceed
        // Just ensure no downstream handler calls preventDefault
    }, true /* capture phase */);

    /* Patch: intercept the body click handler's preventDefault for file labels.
       We bind a capture-phase listener that stops the buggy body handler from
       ever seeing (and preventDefault-ing) this click.
       FIX (bug: "uploading photo/video kicks me out of the gallery, have to
       try several times"): this used to ALSO call inp.click() here as
       "insurance". That was wrong — clicking a <label for="fileInputId">
       already opens the file input's picker natively, with zero JS required.
       stopImmediatePropagation() only blocks other JS listeners; it does NOT
       stop that native label→input behaviour. So every single tap fired the
       OS picker TWICE in the same instant (once native, once from this manual
       .click()) — Android/iOS interpret the second activation as cancelling
       the first, so the picker flashed open and immediately closed, forcing
       repeated taps. Removing the manual click() lets the native behaviour
       fire exactly once, while stopImmediatePropagation() still protects it
       from the downstream handler's preventDefault(). This affected every
       upload button in the app (status, SOS, crisis reports, marketplace,
       complaint evidence, admin news, announcements, profile/cover photos,
       etc.) since they all use <label for="..."> to open their file input. */
    document.addEventListener('click', function(e) {
        var t = e.target;
        var label = t.tagName === 'LABEL' ? t : t.closest('label');
        if (!label) return;

        var forId = label.getAttribute('for') || label.dataset.uploadFor;
        if (!forId) return;
        var inp = document.getElementById(forId);
        if (!inp || inp.type !== 'file') return;

        // Prevent the body handler from seeing this click at all.
        // Do NOT call inp.click() here — the native label click already
        // opens the picker; calling it again double-fires the OS chooser.
        e.stopImmediatePropagation();
    }, true /* capture, runs before all bubbling handlers */);

    /* ── 10. CLOSE / EXIT BUTTONS — capture-phase ──────────────────────────── */
    /* Root cause: body handler checks for .close-modal only when a
       .modal-overlay-container.show ancestor exists. Some close buttons live
       inside overlays that don't have the .show class (display:flex instead).
       Also standalone close buttons like #status-viewer-close are not in
       the selector list at all.

       Fix: capture-phase handler that runs before everything else. */
    document.addEventListener('click', function(e) {
        var t = e.target;

        // ── × on any modal overlay (data-dismiss or .close-modal class) ──
        var closeBtn = t.closest && (
            t.closest('[data-dismiss="modal"]') ||
            t.closest('.close-modal') ||
            t.closest('.close-modal-btn') ||
            t.closest('.modal-close-btn')
        );
        if (closeBtn) {
            e.stopImmediatePropagation();
            var overlay = closeBtn.closest('.modal-overlay-container, .live-sub-modal, [id$="-modal"], [id$="-overlay"]');
            if (overlay) {
                overlay.classList.remove('show','active');
                overlay.style.display = 'none';
            }
            document.body.classList.remove('modal-open');
            return;
        }

        // ── Specific IDs ────────────────────────────────────────────────
        var id = t.id || (t.closest('[id]') && t.closest('[id]').id);
        var closeIds = {
            'status-viewer-close':     'status-viewer-modal',
            'cancel-status-btn':       'create-status-modal',
            'close-auth-modal':        'auth-modal-overlay',
            'status-viewer-close-btn': 'status-viewer-modal',
            'close-status-viewers-panel': 'status-viewers-panel',
            'close-sos-donation-modal':'sos-donation-modal',
            'close-edit-post-modal':   'edit-post-modal-overlay',
            'close-dispute-modal':     'dispute-detail-modal',
            'close-complaint-modal':   'submit-complaint-modal',
            'gallery-close-btn':       'marketplace-gallery-modal'
        };
        if (closeIds[id]) {
            e.stopImmediatePropagation();
            var m = document.getElementById(closeIds[id]);
            if (m) { m.classList.remove('show','active'); m.style.display='none'; }
            document.body.classList.remove('modal-open');
            return;
        }

        // ── Auth modal close (× button inside auth modal) ────────────
        if (t.closest && t.closest('#auth-modal-overlay') && (t.classList.contains('auth-close-btn') || t.textContent.trim() === '×')) {
            var am = document.getElementById('auth-modal-overlay');
            if (am) { am.classList.remove('show'); am.style.display='none'; }
            document.body.classList.remove('modal-open');
            e.stopImmediatePropagation();
            return;
        }

        // ── Click outside modal (on overlay itself) ──────────────────
        if (t.classList && t.classList.contains('modal-overlay-container')) {
            t.classList.remove('show','active');
            t.style.display = 'none';
            document.body.classList.remove('modal-open');
            e.stopImmediatePropagation();
            return;
        }

        // ── Live stream close ─────────────────────────────────────────
        if (t.id === 'live-close-btn') {
            var ls = document.getElementById('go-live-modal-overlay');
            if (ls) { ls.classList.remove('show'); ls.style.display='none'; }
            document.body.classList.remove('modal-open');
            if (typeof window.endLiveStream === 'function') window.endLiveStream();
            e.stopImmediatePropagation();
            return;
        }

    }, true /* capture phase */);

    /* ── 11. HAMBURGER TOGGLE ───────────────────────────────────────────────── */
    function _wireHamburger() {
        var toggle = document.querySelector('.mobile-menu-toggle');
        var sb     = document.querySelector('.sidebar');
        var ov     = document.getElementById('content-overlay');
        if (!toggle || toggle._navWired) return;
        toggle._navWired = true;
        toggle.addEventListener('click', function(e) {
            e.stopPropagation();
            if (sb) sb.classList.toggle('open');
            if (ov) ov.classList.toggle('show');
            document.body.classList.toggle('modal-open', !!(sb && sb.classList.contains('open')));
        });
        if (ov && !ov._navWired) {
            ov._navWired = true;
            ov.addEventListener('click', function() {
                if (sb) sb.classList.remove('open');
                ov.classList.remove('show');
                document.body.classList.remove('modal-open');
            });
        }
    }

    /* ── 12. RESPONSIVE ─────────────────────────────────────────────────────── */
    function _applyResponsive() {
        var mobile = window.innerWidth <= 992;
        var sb     = document.querySelector('.sidebar');
        var toggle = document.querySelector('.mobile-menu-toggle');
        var mc     = document.querySelector('.main-content');
        if (mobile) {
            if (sb && !sb.classList.contains('open')) sb.style.transform = 'translateX(-100%)';
            if (toggle) toggle.style.display = 'flex';
            if (mc)     mc.style.marginLeft  = '0';
        } else {
            if (sb) { sb.style.transform = 'translateX(0)'; sb.classList.remove('open'); }
            if (toggle) toggle.style.display = 'none';
            if (mc)     mc.style.marginLeft  = '270px';
            var mbn = document.getElementById('mobile-bottom-nav');
            // keep mobile nav even on desktop so tablet users aren't stranded
        }
    }

    /* ── 13. BRIDGE QUEUE — drain once app-fixes.js exposes real functions ── */
    window._navBridgeQueue = window._navBridgeQueue || [];
    document.addEventListener('empyrean-init-done', function() {
        // app-fixes.js now exposes its own buildSidebar / navigateTo — let them win
        // but only if they exist. Otherwise our implementations stand.
        if (typeof window._appFixesBuildSidebar === 'function') {
            window.buildSidebar = window._appFixesBuildSidebar;
        }
        window._navBridgeQueue.forEach(function(c) {
            try { if(typeof window[c.fn]==='function') window[c.fn].apply(window,c.args); } catch(e){}
        });
        window._navBridgeQueue = [];
    });

    /* ── 14. INIT ────────────────────────────────────────────────────────────── */
    function _init() {
        _wireHamburger();
        _applyResponsive();
        window.addEventListener('resize', _applyResponsive);

        // Restore last section
        var last = 'dashboard';
        try { last = localStorage.getItem('empyrean_last_section') || 'dashboard'; } catch(e){}
        // Ensure only dashboard is active on first paint
        document.querySelectorAll('.content-section').forEach(function(s){
            s.classList.toggle('active', s.id === (last || 'dashboard'));
        });
        _activeSection = last || 'dashboard';

        // Build sidebar immediately with whatever state is available
        setTimeout(function() {
            buildSidebar();
            _buildMobileBottomNav();
        }, 0);

        console.log('[Nav] app-nav.js v3.0 ready — upload capture, close capture, nav engine active.');
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', _init);
    } else {
        _init();
    }

    // Re-run after user logs in/out
    document.addEventListener('empyrean-user-ready',   function(){ buildSidebar(); _buildMobileBottomNav(); });
    document.addEventListener('empyrean-init-done',    function(){ buildSidebar(); _buildMobileBottomNav(); });
    document.addEventListener('empyrean-section-change', function(e) {
        if (e.detail && e.detail.section) _activeSection = e.detail.section;
    });

})();